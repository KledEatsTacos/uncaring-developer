# Godot-specific ignores
.godot/
*.translation

# C# / Mono-specific ignores
.mono/
bin/
obj/
*.csproj.user
*.sln.DotSettings.user

# Files generated by the editor
# These are usually fine to regenerate.
# If you have issues with team collaboration, you might want to version some of these.
# For a solo project, ignoring them is safe and keeps the repo clean.
export_presets.cfg
override.cfg

# Exported game files
*.exe
*.pck
*.zip
*.dmg
*.apk

# Any personal files you don't want to upload
secrets.txt